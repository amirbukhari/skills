{
  "name": "hydra-tax-apportion-substrate",
  "private": true,
  "description": "Disposable Postgres (ephemeral docker container) + TypeORM substrate for decimal-DEPENDENT billing slices, where decimal columns read back as exact strings.",
  "dependencies": {
    "typeorm": "0.3.20",
    "pg": "8.11.5",
    "reflect-metadata": "0.2.2"
  }
}
