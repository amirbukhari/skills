# §R The requirements register

*PART III — THE REQUIREMENTS REGISTER · [index](README.md)*

**This is the build list.** Every requirement stated anywhere in this document appears here once,
as a single testable sentence with an ID, the check that decides it, and a pointer to the section
carrying the rationale. If a requirement is not in this register it is not a requirement — it is
explanation. If two sections disagree, the register is not the tie-breaker either: a disagreement
is an OPEN question (§Q), and it is listed there.

**How to read a row.** `MUST` / `MUST NOT` is binding. **Check** is how a second engineer decides
whether it holds — a command, an assertion, or a named test file. A check reading *"not yet
mechanized"* is an admission, not an excuse: the requirement stands and the check is missing work.

**Some of it now runs.** `npm run register` (`tools/repo-dsl/verify-register.js`) evaluates the rows
decidable by reading the tree — a constant's value, a symbol's presence, an artifact's contract
validity — and prints `HOLDS` / `FAILS` / `MANUAL` per row. `npm run register:json` emits the same
result as one JSON object for a caller. Exit 0 = all mechanized rows hold, 1 = at least one fails,
2 = the runner could not proceed.

Three things it deliberately does **not** do. It never counts a row it cannot evaluate as holding —
those are `MANUAL`, with the command that would decide them, and **`MANUAL` is not a pass**. It
never runs a mine or a round-trip as a side effect. And a row absent from the runner is not a row
that holds; it is one nobody has mechanized yet. As of 2026-08-31: 13 rows mechanized of ~100,
reporting **9 hold, 0 fail, 4 manual**.

**ID scheme.** `R-<AREA>-<n>`, stable. Areas: MECH (core mechanism) · MINE (mining parameters) ·
REND (rendering/compiling) · COMP (composition) · WIDE (middle tier) · ARCH (archetype hybrid) ·
LANG (language, names, productions) · PAY (payload) · ART (artifact contract) · PIN (corpus pinning)
· CFG (roots, config, wipability) · MEAS (measurement discipline) · TEST (test integrity).

---

## R-MECH — the core mechanism

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-MECH-1 | Pattern discovery **MUST** be LZW dictionary construction over the bottom-up AST node stream. Flat anti-unification / clone detection **MUST NOT** be the discovery mechanism. | The live `.en` compile path loads `generators-lzw.json` through `engine/enlzw.js` and no other generator vocabulary. **HOLDS — measured §Q-2.** | §2 P1, §4A, §5 |
| R-MECH-2 | Every non-leaf dictionary entry **MUST** be an existing entry **plus exactly one symbol** (`m[0]` + `m[1]`). | For each entry, `m[0]` resolves to an earlier entry and `m[1]` is a single symbol. | §5 step 3 |
| R-MECH-3 | The dictionary **MUST** be a DAG: no entry may transitively reference itself. | Promotion rejects a cycle; `hierarchyDepth` is finite for every entry. | §5B cycle safety |
| R-MECH-4 | Discovery, expansion and compilation **MUST** make **zero** model calls. | **Check corrected 2026-08-31 — it named nothing that exists.** It read `foldModelCalls === 0` and `buildModelCalls === 0` "in every published catalog"; those fields live only in `archive/`, so the most load-bearing requirement in the PRD pointed at no field on disk. Now **two halves**, both in `verify-register.js`'s `R-MECH-4` row: **(a)** a grep over the live tree (archive excluded) for any outbound-network or model-vendor call, and **(b)** `modelCalls === 0` on **every registered artifact present**, written by `AC.stamp` (defaulting to 0, refusing a non-numeric) into the **fingerprinted body**, so flipping the number by hand breaks the seal. Neither half suffices alone: (a) cannot see a call through an indirection, (b) cannot see a producer that lies about itself. *Shown to fail:* a planted `gate.json` declaring `modelCalls: 7` in a throwaway corpus flips the row to FAILS. | §2 P1 |
| R-MECH-5 | Every hole **MUST** record the exact source span it abstracted. | `fillOf(template, boundHoles) === ` the site's original bytes, at every admitted site. | §2 P1, §5A |
| R-MECH-6 | Tiers **MUST NOT** be hand-assigned labels; tier **is** dictionary depth. | ARCHETYPE→SKELETON→IDIOM→LEAF is derivable from `hierarchyDepth`, not stored as a tier field. | §2 P4, §5 |
| R-MECH-7 | The flat, holes-are-verbatim path is permitted **only** as a fallback for genuinely-unique one-offs that recur nowhere, and **MUST NOT** stand as a second producer beside the LZW path. | No live code path reads `generators.json`. **HOLDS — measured §Q-2, and more strongly than required: no flat producer exists at all.** `tier` is set to `"recursive"` at one place and `"flat"` nowhere, so the flat counters are a tripwire, not a metric (R-MECH-8). | §2 P4, §4A |
| R-MECH-8 | A retired layer **MUST NOT** be revived as a parallel producer, and the engine **MUST NOT** publish a number that no mine can move. | Every published count is produced by the mine, not by a stale manual run. **One violation found and removed 2026-08-31**: the render printed `flat-fallback 0 (0% fallback)`, which is structurally zero rather than measured (§Q-2). Compare R-ARCH-4 — the same defect in the archetype layer. | §5 (retired statement-idiom layer) |

## R-MINE — mining parameters

**Values are SETTLED. Do not re-open without new measurement.** Values and their source of truth
are in §8.

**The CHECKS were not settled, and three of the four pointed at the wrong file — corrected
2026-08-31.** `MIN_COUNT`, `MAXWIN` and `MIN_SKEL` all live on one line,
`build-lzw-generators.js:59`. The measured values are unchanged (1, 8, 64); only the pointers moved.
This is the more dangerous kind of rot: a wrong value fails loudly, while a wrong pointer sends the
next reader to a retired file that answers a different question confidently. Verified by `grep -rn`
over the live tree and `archive/`, not by reading the prose.

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-MINE-1 | `MIN_COUNT` **MUST** be 1 — a word need not recur. | `build-lzw-generators.js:59` — `+(process.env.MIN_COUNT \|\| 1)`. **Cite corrected 2026-08-31**: this said `engine/compose.js`, which is retired to `archive/engine/compose.js:22` and defines `MIN_COUNT = 2`, so verifying at the old location read 2 and wrongly failed the row. The default is 1 and **HOLDS**; note it is env-overridable, so this binds the default, not every run. | §4B |
| R-MINE-2 | ~~`MAXWIN` is 64~~ — **SUPERSEDED 2026-09-01 by R-MINE-12 (§5D.4C). The ceiling is removed; the default is effectively unbounded.** | It was always *"a ceiling and not a tuned value"*, and measurement showed it cost nothing to remove: enumeration is bounded by the longest stream, so windows are 152,844 at 64 and 153,015 at 128 / 256 / 1024 alike. `maxDepth` is now 76 (= the corpus's 77-statement longest stream − 1) where it was 63 (= MAXWIN − 1). Byte-identity held 1037/1037. *Cite corrected 2026-08-31: `engine/enlzw.js` defines no `MAXWIN`; `archive/engine/patterns.js:69` has 5, from the retired window miner.* | §4B, §Q-6, §5D.4C |
| R-MINE-3 | ~~`MIN_SKEL` **MUST** stay 8.~~ — **AMENDED 2026-09-03: `MIN_SKEL` is 1.** | `build-lzw-generators.js:91` — `+(process.env.MIN_SKEL \|\| 1)`. **Superseded wording, per §9:** *"`MIN_SKEL` **MUST** stay 8."* It was settled on a sweep that never tried 1 and that optimised `netStatementReduction` rather than **review surface**, which is the deliverable. Measured at full corpus scale: review surface **1582 → 1086 top, 20999 → 20214 tree**, byte-identity **1037/1037**, and **zero** hand-authored chunk names orphaned (19 already-orphaned ones re-resolve) — so §10:42's guarantee holds empirically for chunk names too, not just leaf names. **Mechanism:** `skelBytes()` strips `/‹\w+›/g` **before** the floor is applied, so `return ‹expr›;` measures as `return;` = 7 bytes and misses a floor of 8 by one. Only 71 narrow / 118 wide skeletons sat under the floor (0.06%) and they are the **core statement grammar** — a dictionary that cannot spell `return` cannot compose any run containing one. It was a **length filter doing a triviality filter's job**, and for statements those anti-correlate. **Recorded cost:** the old sweep's readability concern is not refuted; it was ruled acceptable on a **sample** of the affected skeletons, not a reading of all of them. *Cite corrected 2026-08-31: `engine/enlzw.js` does not define it.* | §4B |
| R-MINE-4 | `MIN_WORD_CHARS` is 4 — trivial punctuation tokens are not words. | **RETIRED 2026-08-31, not verifiable and not a live requirement.** The constant belonged to the flat composer, which R-MECH-7 records as having no live producer at all. Its only remaining definition is `archive/engine/compose.js:23`, and nothing live reads it. Kept as a row rather than deleted so the id is not silently reused. | §8 |
| R-MINE-5 | Imports and declarations **MUST** be foldable, gated identically to any other statement. | `isFoldable` admits them. | §4B |
| R-MINE-6 | The canonicalizer **MUST** roll a non-refillable sub-expression back to an opaque hole, and **MUST NOT** fail the whole skeleton. | A skeleton containing one bad sub-expression still promotes, with that span as a hole. | §4B |
| R-MINE-7 | **THE LIFT, amended 2026-08-31 (§5D.4).** The renderer **MUST** refuse a whole-run word that is **unnamed or unexpandable**. *It used to say "a file is never one word" — superseded by Amir's statement 6; a **named, drillable** file word is now the target (R-ARCH-15).* | No `.en` renders as a single **opaque** span; a whole-run word carries a name and an `explain` tree. | §4B, §5D.4 |
| R-MINE-8 | **AMENDED 2026-09-01 (Amir's call, §5D.4D).** No **proper sub-span** **MUST** straddle two or more units. A **whole-run** span is **exempt** — it may cover any number of units, and remains gated on `wholeRunOk`. A word still means one thing; for a whole-run word the one thing is the **container** (the file, or one function body), not a definition inside it. | `engine/unit-boundary.test.js`, rebuilt on real corpus source: **827 spans bound by the rule, 0 violations, 309 whole-run spans exercising the exemption**, plus a control proving the exemption rides on `wholeRunOk` and not on `isUnit` being ignored (250 spans / 120 files, 0 admitted without the predicate). *The pre-amendment test asserted this over a synthetic fixture whose symbols are absent from the dictionary, so genSpans returned **zero** spans and the assertion iterated nothing — it was vacuous (§10.3) and is now real.* | §4B (Amir's call), §5D.4D |
| R-MINE-9 | Holes **MUST** stay verbatim TypeScript. | The hole taxonomy in §5C; no hole interior is paraphrased. | §4B, §5C |
| R-MINE-10 | Every un-collapsed body **MUST** be attributed **MINER**, **GATE** or **ARBITRATION**. A bare count is not actionable. | `measure-uncollapsed.js` emits the three-way split. | §4A |
| R-MINE-11 | Structural entry creation **MUST NOT** be gated on recurrence. | An entry records that a composition *exists*; recurrence decides only whether it is REUSED, and whether it is worth naming is a third, separate question — which is what decouples "can we represent this as one word" from "is it worth a model call to name it" (Amir, 2026-09-01). Check: `buildSaturated`'s creation gate is its own parameter (`createMinCount`), defaults to 1, and is never fed `MIN_COUNT`. | §5D.4C |
| R-MINE-12 | No fixed depth/window ceiling may bind below the corpus's own longest stream. | Enumeration is bounded by stream length regardless, so a lower ceiling forbids words without saving work — **measured 152,844 windows at maxWin=64 vs 153,015 at 128, 256 and 1024 alike.** Check: a mine's reported `maxDepth` is a property of the corpus (longest stream − 1), never `MAXWIN − 1`. **Now 76 = 77 − 1, where it was 63 = 64 − 1.** Mechanized in `verify-register.js` as R-MINE-12, replacing the old R-MINE-2 `expected 64` row; it **HOLDS** (76, with 99,923 of headroom) and it **can be shown to fire** — the pre-change artifact reported `dictionaryMaxDepth` 63 against `MAXWIN` 64, which is precisely the `d === maxWin − 1` pinning signature the row now fails on. | §5D.4C, §10.3 |

## R-REND — rendering and compiling

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-REND-1 | `compileFileEn(renderFileEn(src)) === src` **MUST** hold for **every file, always**. This is the floor and it never regresses. | `en-index.json → gate.byteIdentical`; the round-trip tests. | §2 P3, §7.0 gate 1 |
| R-REND-2 | A span **MUST** be swapped to English only when it recompiles to its exact source bytes; anything unverified **MUST** stay verbatim TypeScript. | The per-span byte-exact gate at render time. | §2 P3 |
| R-REND-3 | Selected segments **MUST** tile `[0, len)` exactly, and no two selected spans may overlap. | `checkTiling`. | §5 the fold, §5A |
| R-REND-4 | A readability improvement that loses one byte of identity **MUST** be treated as a regression, not a trade. | Gate 1 is unconditional. | §7.0 |
| R-REND-5 | The `.en` **MUST** be written to `<CORPUS>/sen/files/<rel>.en`; derived `.calc` IR **MUST** go to a gitignored `.cache/`. | §5 on-disk layout; `engine/artifact-location.test.js`. | §5, §8B |
| R-REND-6 | **REWRITTEN 2026-08-31 (§5D.3 note 1).** The **sentence is authoritative**: a hand-edit to a clause's English **MUST** change the compiled TypeScript. The payload is a **derived index**, not the source of truth. *It used to say the compiler MUST NOT read the label region — "names are cosmetic by construction". That made Amir's hand-edit lifecycle (§5D.0 statement 4) a no-op, so it is cut.* | **Cut 1 shipped 2026-08-31 (derive-and-check).** `engine/enfile.js deriveGloss` re-derives the gloss from the payload and **throws** on disagreement, naming both sides; on by default in `enfile.test.js`, opt-in elsewhere (`SDD_DERIVE_CHECK=1`). Mutation-checked: with the check off the same hand-edit is silently ignored, and that is asserted. Measured: **1,037/1,037 real `.en` compile byte-identical with the check on** — zero false positives. **Cut 2 shipped 2026-09-03 for the HOLE LAYER.** `repairFromSentence` inverts the payload's holes from the written sentence and accepts a repair only when re-deriving the gloss from it reproduces the human's sentence byte-for-byte — so an edit is honoured when proved and refused otherwise, never half-applied. Measured on a real `.en`: `` `ESocketEvents` `` → `` `renamedByHand` `` changed the compiled `import`; added prose was refused naming file/written/derived. The structural branch is no longer silent (`deriveStructuralGloss`: an opinion on 9,611/9,611 structural chunks, 0 disagreements on an unedited corpus), and a heading-only edit is ruled a REFUSAL — a heading is computed from its children, so honouring it would rewrite clauses the human never touched; the edit stays expressible at the child. Byte-identity **1037/1037** before and after. *This cell used to read "Cut 2 ... needs the §5E.3.2 grammar parser and is not built."* **Still unbuilt:** that parser — restructured clauses, added prose, and renames of TEMPLATE-supplied tokens (a new word, the miner's job) all still refuse. | §5C, §10, §5E.5 |
| R-REND-7 | Measurement **MUST** run over the whole corpus with a published SKIP set; showcase or demo trees **MUST** be excluded, and per-module results **MUST** include failures. | `write-en-files.js SKIP`; §8 lists the set. | §3 |
| R-REND-8 | A body that is not named **MUST** read as an honest placeholder (e.g. "custom logic (N statements)") and **MUST NOT** be given invented prose. | The `prose.js` honesty rule. | §5 the loop, §5C |

## R-COMP — composition

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-COMP-1 | A generator's `template` **MAY** contain **generator-reference holes** whose fill is another generator invocation rather than verbatim TS. Composition is a first-class requirement, not an optimization. | Schema admits the hole kind. | §5B |
| R-COMP-2 | `expand(gen, params)` **MUST** resolve a generator-reference hole by recursively expanding the referenced generator, terminating at leaves. | `expand` on a depth-`n` composite reaches leaves. | §5B |
| R-COMP-3 | The **fully-expanded** result at a top-level `.en` span **MUST** equal the site's exact source bytes, so every nested level is implicitly gated. | Byte-exact gate applied to the final expansion. | §2 P3, §5B |
| R-COMP-4 | A composite record **MUST** carry its **ordered member ids** and its **depth** (longest path to a leaf, leaf = 0). The **spellings are `m` and `d`** in `generators-lzw.json` and `memberLeafIds` in `mined-library.json`. | `engine/wordlzw-enlzw-fields.test.js` (825 assertions) pins `["id","len","freq","m","d"]` and **forbids** `members`/`hierarchyDepth`. | §5B |
| R-COMP-5 | Each generator-reference hole **MUST** name a `memberId` and the ordered params to pass to it. | Schema. | §5B |
| R-COMP-6 | The manifest **MUST** expose `generators.composites`, `generators.maxDepth` and `generators.compositionEdges`, so **flatness is visible as a regression**. `maxDepth` (deepest span the **live path** emitted) **MUST** stay a distinct field from `dictionaryMaxDepth` (depth of the **mined dictionary**) — conflating them lets a deep dictionary report a renderer that never composed. | `en-index.json → generators`. **Was NOT met until 2026-08-31**: the producer wrote `maxCompositionDepth` and neither of the other two, so R-COMP-7 was comparing `undefined` (§Q-2). All four now emitted by `write-en-files.js`. | §5B |
| R-COMP-7 | `generators.maxDepth` on the **live** `.en` path **MUST** be ≥ 2 and rising. Depth 1 is the degenerate flat path. | `en-index.json → generators.maxDepth`. **Clears the bar on a composition fixture (depth 3, §Q-2); unmeasured on the real corpus — §Q-8.** | §2 P4, §7.3 |
| R-COMP-8 | Promotion **MUST** reject any composite whose `members` would introduce a cycle. | Cycle check at promotion. | §5B |
| R-COMP-9 | The `.en` pass **MUST** emit the **highest-tier** admitted generator for a span, and a composite **MUST** outrank its own members on a coverage tie. | §5A arbitration, extended. | §5B |

## R-WIDE — the middle-tier generator layer

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-WIDE-1 | Every function/method body **MUST** be canonicalized twice — the existing **narrow** axis and the **widened** axis (member-access, method and constructor names become typed holes `‹m›`/`‹ctor›`). | `operations.js fnKey` + the WIDE canon. | §5A |
| R-WIDE-2 | The widened axis **MUST** be an additive, coexisting layer. The narrow axis **MUST NOT** be widened in place — that weakens byte-elimination on structural clones. | Both axes present and separately labelled (`axis: "narrow"` / `"wide"`). | §4 the fix is additive |
| R-WIDE-3 | A body is a middle-tier candidate **iff** its widened key recurs across the corpus with frequency ≥ 2 **and** it is not already claimed by an archetype slot. | Candidate selection. | §5A |
| R-WIDE-4 | Each promoted generator **MUST** carry `id` (`g_<len>_<sha256-10>`), `axis`, `template` (ordered `lit` runs interleaved with typed holes), `holes[]`, `freq` and `filesUsing`. | `mined-library.json → composites[]`. | §5A |
| R-WIDE-5 | `holes[].type` **MUST** come from the closed set `{id, str, num, obj, arr, fn, type, member, method, ctor, args, chain}`. | Schema validation. | §5A |
| R-WIDE-6 | A site **MUST** be admitted only when `fillOf(template, boundHoles)` equals its original bytes. Widened names ride as ordinary string-valued parameters. | The universal byte-exact gate. | §5A |
| R-WIDE-7 | A `type`-name hole **MUST** be admitted only when replacing the type with a `‹type›` hole still yields a byte-exact `fillOf` **at every site** — never on a subjective judgement about whether the type is load-bearing. | The same gate as every other hole. | §6 front 1 |
| R-WIDE-8 | Arbitration **MUST** be deterministic with no coin-flip, in this order: (1) discard any candidate not byte-exact here; (2) widest byte coverage; (3) higher `freq`; (4) **narrow beats wide**; (5) lowest `id` lexicographically. A site claimed by nothing falls back to the statement/data tier, then verbatim TS. | Arbitration is a total order. | §5A |
| R-WIDE-9 | `enfile.js` **MUST** run the generator pass **before** the statement/data passes, since a procedure generator subsumes whole statements. | Pass ordering. | §5A |
| R-WIDE-10 | The pass **MUST** emit a generator span only when the round-trip is byte-exact; otherwise the body stays raw TS. | Span-gated identically to every other pass. | §5A |
| R-WIDE-11 | The manifest **MUST** record `generators.calls`, `.statementsCollapsed`, `.netStatementReduction` and `.filesUsing`. | `en-index.json`. | §5A |

## R-ARCH — the archetype/word hybrid

**Direction SETTLED by Amir; mechanics OPEN (§Q-3). Do not build on a guess.**

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-ARCH-1 | The archetype layer and the LZW word layer **MUST** work together as **one** mechanism, neither subordinate: the word layer supplies the vocabulary, the archetype layer supplies the file-level shape. | The hybrid design, once written. | §5D |
| R-ARCH-15 | A file **MUST** be accounted for by **one top-level word**, whose rendered form **IS** its recursive definition — words made of words down to leaves — and which is **editable at every level**. An opaque whole-file token is forbidden. | Per file: one top word; `explain` yields its full tree; a hand-edit at any depth changes the compiled `.ts`. **Measured 2026-09-01: 1030 of 1037 (99.3%)**, up from 1003 (96.7%) once a stray top-level `;` stopped splitting the run (§5D.4F). The residue is 7 files and is now fully accounted for: **2 are empty files** (1 and 9 bytes, no statements at all — they cannot have a top word under this definition, so the real ceiling is 1035), **4** carry non-whitespace outside the span (the leading-comment case, refused — it broke byte-identity), and **1** has two `interface` declarations the dictionary holds no word for (`no-word`, visible in `audit-rules.js`). | §5D.4, §5D.4F |
| R-ARCH-17 | The renderer **MUST NOT** discard a whole-run word **solely because it covers the run**. | `enlzw.js:121` still enforces the *original* R-MINE-7 (*"a file is never one word"*), which §5D.4 superseded. **HOLDS, 2026-09-01.** The refusal is now conditional on `enfile.chunkGloss(run)` — a whole-run word is emitted only when the run reads as a named chunk (no mechanical repetition, nothing that says nothing), and is still refused and re-segmented otherwise. **Measured: 0/1037 -> 316/1037 files (30.5%) collapse to one word, 1037/1037 still byte-identical, 0 model calls.** Pinned by `engine/chunk-naming.test.js` (34 assertions, gate shown both firing and declining). | §5D.4A |
| R-ARCH-16 | **Review surface** — statements a human must read as **code** (the per-file residual) — **MUST** be reported per file and as a corpus total, beside byte-identity, and is the **headline** success metric. Compression is a mechanism and **MUST NOT** be reported as the goal. | The manifest carries `residualStatements` per file. | §5D.4, §7 |
| R-ARCH-22 | **R-ARCH-15 OUTRANKS R-ARCH-16 (Amir's call, 2026-09-01, §5D.4D).** Where a single word covers a whole file, the renderer **MUST** emit it, **even when a set of nested words would remove more statements**. The ordering is **lexicographic**, not a weighted trade: one-word-per-file first, weighted-interval scheduling for everything else, with **no weight adjusted** — so the fallback objective is bit-for-bit the one it always was. | `enlzw.js genSpans` returns the whole-file candidate directly, before the DP runs. `ONE_WORD_FIRST=0` restores the pure weight objective for measurement: **R-ARCH-15 is 30.6% with it off and 93.1% with it on**, so the row can be shown to bind. *The price was stated when it was paid — review surface 13,873 -> 23,784 — and it is now **repaid**: nested rendering (R-ARCH-19) removed the mutual exclusion this row's ordering was forced to choose between, and the top-level surface is **1,610**.* | §5D.4D |
| R-ARCH-23 | **A re-mine MUST NOT change a `.en`.** §5D.0 statement 2, Amir's words: *"then if I mine the codebase again I should see no change to the .en file because it backwards builds the .en file back into exactly what was written anyways"*. This is **strictly stronger** than the byte-identity every other gate tests: those ask `compile(.en) === .ts` — the machine agreeing with itself in one direction — while this asks whether the **`.en` itself** is stable. | **Two halves, different answers, and they must not be conflated.** **HALF 1, render idempotence** (same source, same dictionary): **MEASURED GREEN corpus-wide 2026-09-01 — 1037/1037, 0 drifted, 0 orphans**, gated by `engine/en-idempotence.test.js` (CORPUS tier, ~5s, 842MB peak); mutation-proven — corrupting 3 of 30 persisted `.en` reported exactly those 3. Nothing had ever compared a fresh render against the `.en` on disk before this. **HALF 2, mine idempotence** — what the row actually says — is **FALSE BY CONSTRUCTION**: ids are allocated as `dict.length` (mining-order position) and **1034 of 1037 `.en` reference one while 0 name a dictionary**, so a re-mine rewrites almost the whole corpus. Decided **statically, never by mining** — a re-mine rewrites the shared 40MB dictionary and invalidates all 1037 `.en`, and a gate that must corrupt the corpus to report is not a gate anyone runs. **RED ON PURPOSE**, like R-PAY-6 and R-ARCH-15: half 2 cannot close until **R-PAY-6 picks a closure** — (a) fingerprint-stamped `.en` or (b) content-addressed ids — which is **Amir's call**, since it moves the payload format corpus-wide. Built ahead of that call so the criterion is mechanized the moment a closure lands. Both verdict branches exercised through the real code path (the HOLDS branch via a scratch corpus). | §5D.0 |
| R-ARCH-19 | A generator chunk **MUST** be able to carry **children instead of a payload**, recursively, to leaves — no depth cap. A file's word, each definition's word, each inner run's word, each straight-line run: every level a sentence with a payload or children beneath it. | Two shapes: ATOMIC `«▶ … ⟪payload⟫»` and STRUCTURAL `«▷ … ⟨children⟩»`. Measured (2026-09-01, after §5D.4F's grouping): **9,617 atomic + 9,611 structural, deepest nest 14** *(was 19,102 + 9,612 when a structural chunk emitted one child per statement)*, byte-identity 1037/1037, 0 model calls. Pinned by `engine/nested-rendering.test.js` (5 assertions, each publishing its own population). `NEST=0` restores the flat renderer for measurement and is exercised as a control. **Amir, 2026-09-01: no cap — "capping at 2 levels just recreates the same 'some code stays raw' problem one level in."** | §5D.4E |
| R-ARCH-20 | **Architecture drift detection (§5F).** The archetype tier **MUST** report, for every **generative** archetype only, how many files conform and how many have **drifted**, where a file has drifted when its top-level tiling leaves any **residual** — a statement its own archetype has no slot for. Conformance **MUST** be reported separately from byte-identical tiling: a file that tiles losslessly and still carries residual is **drifted, not broken**, and merging the two hides drift behind a passing losslessness number. Descriptive archetypes **MUST NOT** be counted — an archetype with no slot schema never claimed a shape to drift from. | `GENERATIVE` is declared once in `engine/archetypes.js` and is the only spelling of the set; `tileTop` types each top-level segment with one of exactly four strings — `glue`, `import`, `preambleType` (the slot-bearing one), `residual`; each generative extractor sets `conforms` from its structural condition **and** `residual.length === 0` **and** `byteIdentical`. Check: the roll-up prints conformance and byte-identical tiling as two numbers, and the non-conformer reason names the distinct residual statement kinds rather than a bare count. **Partly satisfied today** — verified 2026-09-01: `engine/sdd.js` `check()` returns `{scanned, generative, conforming, nonConforming, nonConformers}` and skips descriptive archetypes, so the conform/drift roll-up and non-conformer list exist; the per-archetype breakdown, the aggregated byte-identical count and worst-first ordering do not yet (§5F §3). Run `node build-archetypes.js` — **no figure is pinned here** (§7): the catalogs are absent from the corpus as of 2026-09-01, so any number quoted would be unreproducible. | §5F |
| R-ARCH-21 | A per-file drift answer **MUST** be recomputed from that file's current source when it is asked for, never served from a stored verdict. The persisted archetype artifacts are a cache and a roll-up, not the answer to *"does this file conform right now"*. | A drift check answering from a stale artifact reports the drift of whenever the artifact was last built — the staleness it exists to detect — and does so with a confident number, which is the §8B producer/consumer shape with the dashboard (`GET /api/sdd/check`) as the consumer. Check: the drill-in path re-parses the source and re-runs the extractor; it does not read `sen/archetypes/<rel>.arch.json` for its verdict. **Satisfied today** — verified 2026-09-01 by reading `engine/sdd.js`: `checkFile` re-reads source and re-runs `classifyFile` + `EXTRACTORS`, while `render` is the only path that loads the stored `.arch.json`. | §5F |
| R-MEAS-10 | Where the render is a TREE, review surface **MUST** be published as **both** the top-level read and the whole-tree read, side by side, never one alone. | A tree has no single answer to "how many things must you read", and one of the two always flatters. `en-index.json` carries `reviewSurface.reviewSurfaceTop` (**1,582**) beside `reviewSurface.reviewSurface` (**19,776**), plus `chunks` / `chunksAtomic` / `chunksStructural` / `nestMaxDepth`; `write-en-files.js` prints both on every run. *(Was 1,610 / 29,260 — both moved on 2026-09-01: §5D.4F's stray-`;` fix improved the top read, and grouping a structural chunk's children as RUNS rather than statements cut the whole-tree read by 33%. Publishing only the second would have been the R-MECH-8 failure this row exists to prevent.)* R-MECH-8 applied to a metric that acquired a second honest meaning. | §5D.4E |
| R-UI-1 | A step that a UI watches **MUST** be able to emit its progress as **NDJSON on stdout, one document per line**, with its prose moved to stderr — and **MUST** be byte-for-byte unchanged without the flag. | `engine/progress.js`, schema `sdd-progress/v1`, behind `--json` on `write-en-files.js` and `build-lzw-generators.js`. Check: `engine/progress.test.js` asserts the prose is **identical between modes** (1,515 bytes over the fixture) and that stdout carries **0** JSON documents without the flag. Complements `sdd-run.js` (an envelope after the fact) rather than replacing it — see §5D.5 §2 for why that file's opposing argument was kept. | §5D.5 |
| R-UI-2 | A gate result **MUST** reach a UI as a **first-class event naming its requirement**, never as prose to be parsed, and **MUST** carry the failing units by name. | `gate` events: byte-identity cites R-REND-1, the non-empty mine cites R-PIN-6; `failures` / `problems` are lists, because the file that failed is the one a panel needs to name. | §5D.5 |
| R-UI-3 | The progress event vocabulary is **CLOSED**; an unknown event name **MUST** be refused, not emitted. | `start`, `phase`, `file`, `gate`, `summary`, `end`, `error`. `emit` throws on anything else, and is **exported** so the refusal is reachable and therefore a real guard (§10.3) rather than dead code — pinned by `progress.test.js` with a control proving it does not simply refuse everything. | §5D.5, §10.3 |
| R-DRIFT-1 | Every rule refusal on the byte-exact path **MUST** be recordable, naming **which rule**, **which file and span**, and **why** — a rule that declines **MUST NOT** be able to degrade collapse silently. | `05-architecture.md`: "a catalog with no consumer on the byte-exact path is not a layer; it is drift waiting for an audience." `engine/refusals.js` is that audience: a closed six-reason vocabulary (`no-symbol`, `no-word`, `parts-inexact`, `byte-gate`, `rule-declined`, `gloss-refused`), a sink that is **off by default** so the render path is unchanged, and per-`(rule, reason)` buckets with named span samples. Recording points: `enlzw.js runWord` (all four dictionary reasons), `enfile.js chunkGloss` (per refusing clause), `enfile.js spanActions`' chunk-rule pre-pass. Measured on the corpus 2026-09-01: **295 refused spans across 4 rules in 153 files** — `dictionary:ExpressionStatement` 142, `dictionary:FirstStatement` 135, `generalStmtParts:FirstStatement` 12, `generalStmtParts:InterfaceDeclaration` 6 — byte-identity **1037/1037** unchanged. Check: `engine/rule-refusals.test.js` (27 assertions), `node audit-rules.js`. | §5D.6 |
| R-DRIFT-2 | Catalog drift **MUST** be gated **differentially, against a recorded baseline**, and **MUST NOT** be gated on a refusal counter that cannot fire. | The obvious design — count "the catalog no longer matches these bytes" refusals and demand zero — publishes a **tautological zero** (R-MECH-8) and calls it a guard (§10.3). Measured: `parts-inexact` and `byte-gate` are **unreachable by construction**, because `runWord` proves every statement canonicalizes before calling `windowParts`, and every part list is self-verified `fillOf === exact source slice` with gaps carrying literal trivia. The deeper fact: the LZW dictionary is keyed on canonical **symbols** and never supplies bytes, so a stale entry cannot produce a wrong file — only fail to match, surfacing as an ordinary `no-word`. Drift is therefore observable **only as a change**: `audit-rules.js --write-baseline` records per-rule counts, and a rule whose count **rose** is one that used to match this corpus and stopped. A count that **fell** is collapse improving: reported, never failed. | §5D.6, §10.3 |
| R-DRIFT-3 | A reason that cannot fire **MUST** be published as **unreachable, with the argument for why**, never as a passing zero. | `refusals.js UNREACHABLE` carries the proof text; the report prints `UNREACHABLE BY CONSTRUCTION — …` in place of a count's meaning, and `rule-refusals.test.js` asserts **both** that the count is 0 **and** that `reachable === false`. So the day someone reorders `runWord` and makes them reachable, the claim fails a test and gets rewritten rather than quietly becoming false — R-MECH-8 applied to the guard's own dead branches. | §5D.6, §10.3 |
| R-REND-9 | A chunk's sentence **MUST** be produced by exactly the function that re-derives it for checking. | R-REND-6 compares the written sentence against `namedLabel \|\| genLabel`; a renderer using any other order fails its own check. Measured on the corpus before the fix: *"await find one or fail into `payment`"* written, *"await await into `payment`"* derived — the §8B producer/consumer drift shape with R-REND-6 as the consumer. Check: the nested renderer's `label()` calls `namedLabel \|\| genLabel` and nothing else; `enfile.test.js`'s corpus case runs with `deriveCheck` ON. | §5D.4E |
| R-ARCH-2 | An archetype's slots **MUST** be filled by **mined words**, not by hand-authored sub-grammars, wherever the miner already succeeds. | Slot fills resolve to dictionary entries. | §5D |
| R-ARCH-3 | Everything an archetype emits **MUST** pass the **same** byte-exact gate as any other span, with **no silent fallback**: a slot that cannot refill exactly leaves its span as verbatim TypeScript, loudly. | One producer, one gate. | §5D constraint 1 |
| R-ARCH-4 | Archetype correctness **MUST** be measured by refilling slots from the dictionary and comparing to original bytes. A `byteIdentical: 100%` derived from `checkTiling` — which re-slices and rejoins the source — **MUST NOT** be cited as a generation check; it is a tautology. | A real generation check, not `rebuilt = segs.map(s => src.slice(s.a,s.b)).join("")`. | §5D constraint 2 |
| R-ARCH-5 | Where an archetype slot and a mined word claim overlapping bytes, one **deterministic** rule **MUST** decide. | Arbitration order, per R-WIDE-8's model. | §5D constraint 3 |
| R-ARCH-6 | **CORRECTED 2026-08-31 — the extractor was not broken; the requirement was a misreading.** An archetype extractor's slot bag is `result.slots`, and per-column identity is `.prop` (TS property) with `.parsed.name` (db column). *It used to say `extractEntity` "returns `className`, `table` and per-column `.name` as undefined" and MUST be fixed before anything relies on it — that describes a reader using top-level keys, which its own Check line admitted: "read the stored keys, not the returned ones." Measured against the §5D.1 reference case: `conforms: true`, `byteIdentical: true`, `className: "PaymentPlan"`, `table: "payment_plans"`, 5 columns, 2 relations — i.e. the panel's own "entity PaymentPlan, 5 cols, 2 rels".* | `engine/archetypes.test.js` — the reference-case block, one assertion per sentence clause. | §5D constraint 4 |
| R-ARCH-18 | A relation slot **MUST** carry every fill its sentence needs as a **named field** — `kind`, `target`, `inverse`, `join` — read from **all** decorators on the member, not just the first. | `engine/archetypes.test.js`: `relations[0].parsed.join === "account_id"`; a bare `@JoinTable()` yields `join === true`, distinguishing "implied name" from "no join". | §5E.2, §5E.4 |
| R-ARCH-13 | **AT-ARCH-1, now a live check and not only a test.** For an archetype-generated file, mining and re-rendering it **MUST** return the authoring sentence byte-identically, and the generating command **MUST** verify this **before writing** and refuse the write on any difference. | `engine/entity-sentence.test.js`: both legs plus the **fixpoint turn** (a normaliser that is not already at its fixpoint moves on turn two), and each refusal shown to fire. `new-archetype.js` exits 2 with `stage: "AT-ARCH-1"` and writes nothing. | §5E.2, §5E.3.6 |
| R-ARCH-14 | The archetype command **MUST** emit the `.en` **first** and derive the `.ts` from it — never the reverse — and **MUST** be drivable non-interactively (no prompts; one JSON document on stdout). | `new-archetype.js`: `.en` is written before the `.ts`, so a crash between the two leaves the English and never an orphan `.ts`; `--json`, `--stdin`, `--dry-run`; prose to stderr only. **Zero model calls** (R-MECH-4). | §5E.3.6 |
| R-ARCH-7 | Panel-quality **MUST** count only bytes on the round-tripping path. An archetype that reads beautifully and does not compile back byte-exactly contributes nothing. | §7.2 definition. | §5D constraint 5, §7.2 |
| R-ARCH-8 | Residual top-level code under an archetype **MUST** be reported, never absorbed to inflate conformance. | Conformance gate. | §5 tier 1 |

## R-LANG — language, names and productions

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-LANG-1 | A rendered clause **MUST** be produced by one **grammar**: a name is a nonterminal's **spelling**, a production is its **expansion** (§5E.3.5). Both layers persist as artifacts; it **MUST** be knowable which owns a site. | §5C's table, read as spelling + expansion of one nonterminal. | §5C, §5E.3.5 |
| R-LANG-2 | A name's key **MUST** be `sha256(canonical skeleton)[0:16]`, axis-prefixed — **never** the word's dictionary id, which is an artifact of mining order. | `engine/word-names.js`. | §5C, §8 |
| R-LANG-3 | Mining-parameter changes **MUST** orphan nothing; a canonicalizer change **MUST** orphan exactly the skeletons it altered. | Retune `MAXWIN`/`MIN_COUNT`/`MIN_SKEL` → zero orphans. | §5C |
| R-LANG-4 | A leaf **MUST** be named only where `spanProse` has nothing site-specific to say. A static name where a production could quote the real identifier and callee is a **regression**. | The admission rule, decidable per site. | §5C |
| R-LANG-5 | A name whose skeleton no longer exists **MUST** move to the `orphans` ledger and **MUST NEVER** be deleted. | `word-names.json → orphans`. | §5C |
| R-LANG-6 | Before generating a new name the authoring pass **MUST** match against orphans first. | Authoring pass order. | §5C |
| R-LANG-7 | A re-adoption **MUST** be written to the rename queue as a **proposal**, scored by token edit distance, and **MUST NEVER** be applied automatically. Auto-re-attachment is the drift bug in a new costume. | `<corpus>/.cache/spec-derived/name-queue.json` (`AC.pathFor("name-queue")` — the kind is registered and stamped, not hand-written); a human is the consumer. *Path corrected 2026-08-31: no `results/` directory exists anywhere; the kind is `home: "cache"`.* | §5C |
| R-LANG-8 | Rename-queue length **MUST** be reported beside byte-identity, as information — **never minimised** as a target. | §7.0 gate 4. | §5C, §7.0 |
| R-LANG-9 | Whether a hole is **word-like** (quoted verbatim into the clause) or **code-bearing** (left as code) **MUST** be a per-site predicate on the hole's contents evaluated at render time — **never** a policy attached to the hole's type. | The English-completeness scanner is the mechanical form of the predicate. | §5C, §7.0 gate 3 |
| R-LANG-10 | When a production cannot say something **true** about a site it **MUST** emit the vacuous clause and that site **MUST** be counted. A vacuous clause is retired by saying something true — **never** by rewording the placeholder to escape the frozen list. | §7.0 gate 2 + the frozen list. | §5C |
| R-LANG-11 | **TIGHTENED 2026-08-31 (§5D.3A).** An LLM **MAY** produce **the SPELLING of a nonterminal — a name — and nothing else.** Productions, slot boundaries, connectives, alternative selection and hole fills are **produced by code** and are outside a model's reach. Every rename **MUST** be gated on **structural identity modulo names**, byte-identity, and coverage invariance. Nothing correctness-relevant may come from a model. *It used to read "names **and grammar surface** only" — a phrase defined nowhere in this document, which read loosely would let a model author the sentence shapes themselves and put the SYNTAX of the English inside its blast radius rather than the vocabulary. The loose reading is retired.* | `refine-language.js`: the model may return only `[{index, name, rationale}]`; `renderProduction(c)` derives a production from the entry's **role signature**; `structuralSkeleton()` strips `name`/`minedName`/`namedBy` and compares the rest, refusing with *"refined library changed the mined structure — refusing (step must touch names/metadata only)"*. | §2 P2, §5D.2, §5D.3A |
| R-LANG-16 | Phrasebook rules **MUST** be keyed to the target language's **AST node kinds**, one rule per kind — **never** to shapes mined from a corpus. Cardinality and optional syntax are **parameters of a rule** (a list-join), never grounds for a second rule. | The rule table's keys are `ts.SyntaxKind` names; a key that is a mined shape string fails review. Progress is `kinds with a rule ÷ kinds in the language` — a fraction that can reach 1, unlike a mined table. **Measured 2026-08-31:** the corpus exercises **100** of TypeScript's **400** `SyntaxKind` values; **28** kinds cover 90% of node instances against **437** mined shapes for the same coverage. | §5D.3C |
| R-LANG-17 | Rules **MUST** compose **recursively** — a parent rule renders by applying the rule set to each child and stitching per its own template — and a node kind with **no authored rule MUST fall back** to today's unfolded output, never fail the render. | A rule never inspects what its children are, only that they render. Adding the first rule ships alone and regresses nothing; an unruled kind is reported as residual under R-LANG-10's honesty rule, not hidden. | §5D.3C |
| R-LANG-18 | The naming stage **MUST** be able to name a **word** (a multi-statement chunk) as a unit, not leaf slots only. | Naming has two levels (§5D.3D): the chunk, named by the LZW mine, and the leaf, named into a node-kind rule slot. Check: a word of arity >= 2 carrying a whole-chunk name renders as ONE clause and the `.en` still compiles byte-identically. | §5D.3D |
| R-LANG-19 | **AMENDED 2026-09-01: a whole-chunk name is a HEADING OVER the hole-filled content, never a REPLACEMENT FOR it.** A chunk name is rendered FIRST, so a recurring run still reads as one recognised pattern (§5D.3D's decision stands); the clauses the node-kind rules produced follow it, composed exactly as they would be with no chunk name at all. A chunk name is therefore purely ADDITIVE — it can only make a label say more. Supersedes *"a whole-chunk name MUST take precedence over member composition"*, which was implemented as `if (whole) return whole`. | The original wording was written about READING ORDER and got implemented as SUBSTITUTION. Measured at `a736295` by naming all 233,248 composites at once: substitution takes the corpus from **77,766 concrete identifiers to 33,229 — 44,537 gone, 57%** — while the amended rule loses **0** and leaves the clause count and all 1,037 byte-identity checks unchanged. This is the leaf-tier pilot's defect (27,673 -> 7,644) one level up, and it is worse there, since s11 measured that 63% of the corpus's identifiers live in d>=1 chunk labels. **A name is a label alongside the content, at BOTH tiers** — the same rule as R-LANG-23, stated for the composite case. **Validated on a real batch, not only synthetically:** 20 chunk names at d=1, one model call, applied — corpus-wide identifiers 77,766 -> 77,766, clauses 45,768 -> 45,768, 1037/1037 byte-identical, 18 files reading differently (§5D.3D §7). Check: `chunk-naming.test.js` pins that a chunk-named label ENDS WITH the un-chunk-named one (additive, never editing) and that a chunk name with no leaf names still carries the rules' clauses; naming-gate check 4 fails any batch that loses an identifier. | §5D.3D, §5D.3A |
| R-LANG-20 | Names **MUST** be assigned in **ascending depth order**, leaves (d=0) first; a word **MUST NOT** be named before every word in its `m` chain is named. | Every composite is `prefix + one leaf` — **0 violations, re-measured 2026-09-02 across 115,832 (wide) / 126,338 (narrow) entries on the post-MAXWIN-fix catalog** (maxDepth 76; the pre-fix reading was 115,661 / 126,167 at maxDepth 63 — the ceiling removal does not touch the invariant) — so the dependency relation is a TOTAL ORDER along each chain and a name assigned out of order cannot be grounded in named parts. Check: the naming producer sorts by depth ASC and enumerates a word's components, not only top-level emitted words; `count` orders rows only *within* a depth tier. **SATISFIED 2026-09-01 by `name-words.js` + `engine/naming-plan.js`** — `orderViolations()` refuses a plan with one ungrounded word, and `name --tier N` (N>0) refuses to run while any leaf in the plan is unnamed (§5D.3F). The old worksheet producer `name-words-lzw.js:89` still sorts depth DESCENDING; it is superseded, not the producer. | §5D.3E, §5D.3F |
| R-LANG-21 | Leaf skeletons (d=0) **MUST** be inside whatever naming scope is chosen. | Every chain bottoms out at a leaf, and the tail of every deep word is leaves (**2,659 of 2,659** appended halves measured). A scope that excludes d=0 — as "name depth 1–8" does — leaves the base of every word unnamed. Check: no naming scope excludes d=0 — `naming-plan.tiersOf` throws unless the caller states the leaves are already named, and the CLI refuses a composite tier while any leaf skeleton is unnamed. **REAFFIRMED against the rule-coverage filter (§5D.3G): a rule-covered leaf stays IN the plan and is still accounted for — by code rather than by a name. The filter decides WHO accounts for a leaf, never WHETHER.** | §5D.3E, §5D.3F, §5D.3G |
| R-LANG-22 | The naming target is **leaves + used shallow words = 1,414 + 638 = 2,052 names**, of which **only 28 leaves are worth a model call** — 98% of the leaf tier is already rendered by node-kind rules and naming those REGRESSES the corpus (§5D.3G, measured) — **pinned to commit `8882830`** (re-measured 2026-09-02; supersedes 2,767 + 3,094 = 5,861 at `216f928`, which superseded 2,619 + 2,789 = 5,408). **The figure MUST carry the commit it was measured at** — it is a function of the render, not a constant, and it has moved three times in one day. It **MUST** be stated as a cost, never as a saving. | It is **more** names than today's 1,018, and that is the honest price of R-ARCH-15's *"words made of words down to leaves"*. What it buys is completeness: at 2,052 every used word is either named directly (d<=8) or is a named prefix plus named leaves (d>=9) — guaranteed by the 0-violations result, so there is no third case and the 380 deep words (d>=9, measured at `8882830`) need no names of their own. Check: any published naming plan states both figures and does not present depth-bounding as a reduction — `naming-plan.summarize()` cannot report the target without today's figure beside it. **Provenance of the whole chain is in §5D.3F §2:** 5,408 was measured over 943 files against `en-index`'s own `totalFiles: 1037` and pre-LIFT; the MAXWIN removal accounts for exactly **1** name (5,862 -> 5,861, measured either side of `216f928`); and **`8882830` (one word per file, 30.6% -> 93.1%) is what took it to 2,052** — a file that renders as one word emits one span instead of five, so far fewer distinct words are emitted and d>=9 absorbs most of them. Each figure comes from the render's own span set and was cross-checked against `en-index.json`'s `generators.calls` (4,787 then, **1,135** now). **STALE as of 2026-09-01:** R-ARCH-18 cut the span set to **1,135**, so the *used-word* half of this target must be re-measured; the leaf half is unaffected. Re-measuring it is queued, not done — the figure above is retained rather than guessed at. Direction and the ~1.64x ratio are unchanged. | §5D.3E, §5D.3F |
| R-LANG-23 | **A name is a label, not a structure.** Segmentation and clause structure are computed from the UNNAMED dictionary; names are applied afterwards. `namedLabel` takes its clause list from `spanActions` over the whole run and substitutes a name only into a clause covering EXACTLY ONE statement; `spanActions` publishes `covers[i]` so this is checked, not assumed. Enforced by naming-gate check 5 (fold invariance): a file's emitted clause count may DECREASE (the adjacent-identical collapse, R-LANG-16) but never INCREASE. | Measured twice, green on every gate then in force: composing one clause per statement dissolved rule folds wherever a run contained a named leaf — import repeats inside one clause 1 -> 284, emitted clauses 45,767 -> 46,055, with bytes, payloads, coverage and every quoted identifier intact. §5D.3A constrained what a model may SUPPLY and said nothing about what a name may DISPLACE. Check: `chunk-naming.test.js` pins `covers` on a folded import run; `naming-gate.test.js` shows check 5 firing on 1 -> 3 and allowing 3 -> 1. **NB** the check Amir specified — word-id sequence identical with and without names — already existed as gate check 2 (a payload is `lzw1 n<id>⟨holes⟫`) and had been passing throughout; segmentation was never name-sensitive. | §5D.3A, §5D.3G §10 |
| R-LANG-24 | **A call names its RECEIVER.** An expression statement calling a method renders as ``call `set` on `cacheProxy` `` — callee and receiver quoted verbatim (§3's rule for declarations, applied to calls), not de-camelCased. Restricted to receivers `dottedText` accepts (a plain dotted name), so `expect(items).toHaveLength(3)` declines to the older clause rather than quoting an expression as if it were a variable. **A name is not a permanent asset:** when a rule overtakes a skeleton, the name authored for it MUST be retired (`name-words.js retire`), and retirement is measured, never assumed. | `firstCallName` returned only the METHOD, so `cacheProxy.set(...)` and `Decimal.set(...)` both rendered "call set" — the same six characters for two unrelated operations, with the distinguishing word present in the source and discarded. 3,595 statements in the foldable scope call a method on a receiver. **Both defects the naming pilots surfaced were this one:** a model asked to name `‹id›.set(‹args›)` answered "set a configuration value", correct for `Decimal.set` and wrong for `cacheProxy.set` — no hole-free name can be right for a skeleton whose receiver varies, only a hole can. Measured at `1b20b62`: corpus identifiers **77,766 -> 82,975 (+5,209)**, 1037/1037 byte-identical, one-word-per-file unchanged at 96.7%, namable leaves **20 -> 6**, and the 14 authored names it overtook went from best-available to **1,768 identifiers worse than nothing** — retired, measured as a gain. | §5D.3G §11, §5D.2 |
| R-LANG-15 | The grammar shell **MUST** be **derivable from the mined structure alone**: for any dictionary entry, its production **MUST** be a pure function of its role signature, so that two runs of the naming stage over the same corpus differ **only** in spellings. | `renderProduction(c)` takes only `c.roles`; the structural-inertness gate proves the rest did not move. **A naming run that cannot be shown to leave the structure identical is refused, not reported.** | §5D.3A |
| R-LANG-12 | The pipeline **MUST** have exactly two kinds of step: a **deterministic mine** (zero model calls) and a **scripted LLM naming stage**. The naming stage **MUST** be an invocable script in this repo — never ad hoc, never a chat transcript. | `npm run name` runs it end to end; `npm run mine` makes zero model calls. | §5D.2 |
| R-LANG-13 | The naming stage **MUST APPLY** names, gated mechanically (byte-identity + coverage invariance + grammar injectivity) — **not** emit a worksheet and wait for a human. *It used to be worksheet-only; superseded by §5D.0 statement 5.* Orphan **re-adoption** remains a proposal (R-LANG-7). | `name` writes `word-names.json`; the gate is the consumer. | §5D.2, §10 |
| R-LANG-14 | Every pipeline script **MUST** be callable non-interactively with structured output (JSON on stdout or a stamped artifact) so a UI can drive it unchanged. No blocking prompts. | Each `npm run` target runs headless in CI-shaped invocation. | §5D.2 |

## R-PAY — payload encoding

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-PAY-1 | Payloads **MUST** be plain readable UTF-8 text, never an opaque blob. The live form is `lzw1 <axis><wordId>⟨hole⟨hole…`. | `engine/payload.js`; `engine/dialect-guard.test.js`. | §7A.1 |
| R-PAY-2 | Any future encoding **MUST** be checked against the failure mode an opaque payload has: it **scales with success**, so improving the miner actively degrades the artifact. | Design review against that property. | §7A.2 |
| R-PAY-3 | Sentinel safety **MUST** be structural: an encoded payload **provably** contains none of `« » ⟪ ⟫ ▶ ⟨`, by escaping — never by an assumption about what the corpus happens to contain. | Escaping is total; the guard test asserts it. | §7A.3 |
| R-PAY-4 | `decode()` **MUST** be fail-closed: wrong tag, bad axis, missing id or unknown escape all throw, naming a stale superseded encoding specifically. | `engine/dialect-guard.test.js`. | §7A.4 |
| R-PAY-5 | Hole dedup via a shared fill table, and parameter hoisting, are **REJECTED**: both compress further and both replace visible source text with an indirection a reader must resolve by hand. Residual negative compression from gloss prose is honest and acceptable. | Neither is implemented. | §7A.5, §3 |
| R-PAY-6 | **A word id is not stable across a re-mine, and the payload references word ids.** A `.en` is therefore decodable only against the dictionary it was rendered with. The engine **MUST** close this either by (a) each `.en` naming the dictionary `fingerprint` it was rendered against, with `compileFileEn` **REFUSING** on mismatch, or (b) making ids content-addressed as skeleton names already are (strictly better, strictly more work). | Today's harm is bounded because the `.ts` is authoritative and a `.en` can always be re-rendered; the failure mode is a compile producing **wrong bytes, not an error**. | §1B.5, §8B |

## R-ART — the artifact contract

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-ART-1 | The engine tree is **engine code + PRD only**. It **MUST NOT** hold bytes derived from anyone's corpus; its remote is public and the corpus is not. | `engine/artifact-location.test.js`: no corpus-derived file on disk in the engine tree. | §8B |
| R-ART-11 | A stamped artifact **MUST** carry a `contentFingerprint` that is **equal across two runs producing the same content** — wall-clock, interpreter version and the regenerate command line (which embeds absolute paths) are **excluded** from it by a declared `VOLATILE` list. The tamper seal `fingerprint` keeps covering everything, `contentFingerprint` included. | `AC.contentFingerprintOf`; two stamps differing only in `minedAt`/`node` agree on it, a stamp differing in real content does not, and hand-editing it breaks `fingerprint`. **Why:** re-mining the same corpus at the same settings produced a different `fingerprint` — only `minedAt` and the seal over it moved — so nothing on an artifact could distinguish **stale** from **wrong**. R-MEAS-2 read as a live failure against a merely stale manifest on 2026-08-31 for exactly this reason. | §8B |
| R-ART-2 | Every artifact **MUST** resolve from the corpus root through `AC.pathFor(kind, corpusRoot)`. No source line may name a corpus artifact relative to `__dirname`. | Guard assertion 4. | §8B |
| R-ART-3 | `tracked` artifacts **MUST** live at `<corpus>/sen/catalog/`; `cache` artifacts at `<corpus>/.cache/spec-derived/`. Root `catalog/` is forbidden for tracked artifacts because the corpus `.gitignore` ignores it and the file would be silently untracked. | `AC.HOMES`; guard assertion 2. | §8B, §1B.4 |
| R-ART-4 | Every artifact **MUST** carry the header `schema`, `artifactVersion`, `corpus` (corpus-pinned kinds), `generated`, `fingerprint = sha256(canonical body without header)[0:16]`. | `AC.stamp` is the only publisher; `AC.validate` refuses a missing or wrong fingerprint. | §8B |
| R-ART-5 | `schema` **MUST** be bumped on **any** shape change, and the registry's `requires` **MUST** name the top-level keys a consumer actually reads, so a same-version shape change is caught too. | The registry table in §8B. | §8B |
| R-ART-6 | A consumer that cannot verify what it is reading **MUST REFUSE**, naming what it expected and what it got. `catch { return null }` is the bug class, not the safety net. | No silent fallback anywhere downstream. | §8B |
| R-ART-7 | Validation **MUST** be the default read path: `AC.load` is the only read helper, so a new consumer must go out of its way to be unsafe. A genuinely-correct fallback is passed explicitly as `{optional: true}` and returns a **reason**, never a bare null. | `artifact-contract.js` exports no unvalidated read. | §8B |
| R-ART-8 | Compose-layer composites carry **no `id`**; they identify by `name` (`g_<len>_<6hex>`) plus `entryId`, and only `leaves` carry `id` (`p_<8hex>`). Consumers **MUST** use `AC.idOf(record)`. | Keying a composite on `.id` yields `undefined` for every one of them. | §8B |
| R-ART-9 | The id spaces **MUST** stay disjoint: `word-names` keys are `w:`/`n:<16hex>` over the LZW dictionary; compose-layer leaves are `p_<8hex>`. **Names key the LZW dictionary; panel surfaces key the compose layer** — a naming UI must read the LZW dictionary directly. | §8B composite id contract. | §8B |
| R-ART-10 | `word-names` entries **MUST** be the v1 shape `{sym, en, sites, named}` keyed by `sha256(sym)[0:16]` axis-prefixed. The v0 `{name, hint, tier}` shape is retired and its producers archived. | §8B registry. | §8B |

## R-PIN — corpus pinning

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-PIN-1 | Every generated artifact **MUST** carry a corpus stamp written **on the artifact**, never inferred from its path or filename. A filename is not provenance. | `corpus` header field. | §8C.1 |
| R-PIN-2 | There **MUST** be one publisher, and it **MUST** refuse to publish a library whose declared `corpus` is not the tree it is being published into. It writes the artifact **beside the corpus it describes**. | Publisher refusal. | §8C.2 |
| R-PIN-3 | A consumer **MUST** refuse a non-matching artifact and **MUST NEVER** fall back: it returns an honest miss naming what it looked for and where, and renders nothing. | `AC.load(kind, AC.pathFor(kind, selected), { corpus: selected })`. | §8C.3 |
| R-PIN-4 | An **absent** stamp is UNKNOWN, not WRONG: the artifact is unusable *for reporting* until republished, and **MUST NEVER** be silently adopted as an answer. | `allowUnstamped` is explicit, never default. | §8C.4 |
| R-PIN-5 | Version shadowing **MUST** filter by corpus **first**, then take the highest `vN` (unversioned sorts lowest). Version rank **MUST NEVER** override provenance. | Rank applied before provenance is exactly how a correct mine gets shadowed by a stale one. | §8C.5 |
| R-PIN-6 | An artifact **MUST** declare only what it carries: the publisher refuses when a summary count disagrees with the body or cannot be verified, and a build that cannot walk the whole tree **MUST** fail loudly or mark itself `complete: false`. Silent under-reporting is banned. | Publisher assertion. | §8C.6 |
| R-PIN-7 | A consumer **MUST** take its roots as **one selection**. There **MUST NOT** be a second independent setting for one fact — keeping two paths equal by discipline is not an invariant. | Resolution and validation take the same root from the same source. | §8C, §1B.1 |

## R-CFG — roots, configuration and wipability

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-CFG-1 | There **MUST** be exactly two roots: `SOURCE` (**read** — the `.ts` tree, never written by any tool) and `CORPUS` (**write** — holds `sen/` and every derived tree). | §1B.1. | §1B.1 |
| R-CFG-2 | The two roots **MUST** be independently settable and defaultable, with no crosstalk: same directory (self-hosting, the default) and different directories (render a fork into a fresh tree) must both work. | `engine/corpus-root.test.js`: setting one never moves the other. | §1B.1 |
| R-CFG-3 | Precedence **MUST** be resolved per root in exactly one module: `--source/--corpus` flag > env var > `<engine>/.env` > engine-relative default. | `engine/corpus-root.js` is the single resolver. | §1B.1 |
| R-CFG-4 | A root that is **set but missing MUST refuse loudly**, naming the root, the resolved absolute path, and **which layer supplied it**. There is no silent fallback to a default when an explicit setting is wrong. | Refusal message content. | §1B.1 |
| R-CFG-5 | Repointing either root **MUST** be a **one-file change** — one line in `<engine>/.env`. *"if you need to make more than 1 file change to alter the directory we are pointing at then we have done this wrong and need to fix it"* (Amir). | `corpus-root.test.js` greps the live tree and fails if a root literal or a second spelling appears outside the resolver and `.env`. | §1B.6 |
| R-CFG-6 | `sen` **MUST** be spelled in exactly one place (`LAYOUT.sen`) and consumed everywhere else through `CR.senDir()` / `AC.HOMES.tracked`. It is a folder name, **not** a root, and **MUST NOT** become configurable. | The grep guard. | §1B.2 |
| R-CFG-7 | Wiping `sen/` **MUST** require an explicit flag the user types (`--wipe-sen` **and** `--go`) — never a default, never silent, never a side effect of a cheaper cleanup or of an engine change alone. | `sdd-clean.js` gate. | §1B.3 |
| R-CFG-8 | **No flag = refuse**, and the refusal **MUST** name what it would have deleted, with file and byte counts, so the cost is visible before it is paid. | Dry-run output. | §1B.3 |
| R-CFG-9 | `SOURCE` **MUST NEVER** be wipable by any tool, and the protection **MUST** hold structurally even in the self-hosting case where `SOURCE === CORPUS` — not by the cleaner happening to be pointed elsewhere. | `sdd-clean.js` refuses at plan time, before any `rm`. | §1B.3 |
| R-CFG-10 | The wipe **MUST NOT** touch `<CORPUS>/catalog/`, the legacy STEP-4 tree. It is a separate, still-undetermined question (§Q-4). | `PROTECTED` name list. | §1B.3, §1B.4 |
| R-CFG-11 | A tool that deletes a tree **MUST NOT** live inside that tree, and a fresh corpus **MUST** need no scripts copied into it. | `sdd-clean.js` lives in the engine. | §1B.3 |
| R-CFG-12 | SOURCE-PROTECTED artifacts (§8A) **MUST** be tracked in the corpus's own repo and **MUST NEVER** be classified as regenerable-cache, gitignored away, or deleted in any cleanup. A cleanup that cannot tell them apart **MUST** stop and ask. | §8A list. | §8A |
| R-CFG-13 | **A refusal MUST NOT present as a crash.** Every guard that DECLINES a run **MUST** exit 3 with its reason as prose; only genuine faults exit 1, and they **MUST** keep their stack. The fault handler **MUST** be installed **before the roots resolve**, or a misconfigured root bypasses it entirely. | `sdd-clean.js`'s `Decline` class and its single `uncaughtException` handler. Behaviour pinned by `engine/sdd-clean.test.js` (declines -> 3, faults -> 1), both mutation-checked; structure by `verify-register.js`. | §1B.3 |

## R-MEAS — measurement discipline

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-MEAS-1 | Every metric **MUST** be computed by one committed command reading one field of one committed artifact. **No metric is computed by eye**, and "done" **MUST** be a number a second engineer can reproduce. | §7.3. | §7.3 |
| R-MEAS-2 | Every English-coverage and statement-collapse ratio **MUST** use the **enfile-layer** total as its denominator; a compose-layer figure **MUST** be labelled as such, and the two **MUST NEVER** be mixed inside one ratio. **The denominator is the SAME WALK as the numerator** — §7.3's settled `S`, the direct children of every `Block` or `SourceFile`, exported once from `enfile.countBodyStatements`. | `verify-register.js` R-MEAS-2, four teeth: **(a)** the accounting identity `collapsed ≤ S`, `residual = S − collapsed` **unclamped**, `reviewSurface = calls + residual`; **(b)** `countBodyStatements` still walks `Block|SourceFile`, so a quiet narrowing back to function bodies reopens the hole that published a perfect score; **(c)** it is **exported**, so a second consumer reuses S instead of defining it again; **(d)** nothing divides by `operations.fnStmtCount`, which is a per-function **cluster size** (one consumer, `measure-operations.js`) and was never the corpus denominator §7.3 once named it as. *Shown to fail:* narrowing the walk and dropping the export each flip the row to FAILS. **HOLDS 2026-08-31: collapsed 22,760 ≤ S 33,918; residual 11,158 unclamped; reviewSurface 16,889 = 5,731 + 11,158.** | §4, §7.3 |
| R-MEAS-3 | The un-collapsed classifier **MUST** stay frozen: (a) WIDE key recurs with freq ≥ 2; (a2) placeholder density **`holes / N < 0.5`**, a **strict** comparison; (b) not covered by a generator span in that file's `.en`; (c) not claimed by an archetype slot. The metric is the **count of files containing ≥ 1 such body**. | `engine/uncollapsed-density.js` + its mutation-checked test. | §7.3 |
| R-MEAS-4 | **(a2) MUST NOT be dropped.** Without it, a body whose every statement fails to generalize keys as all-placeholders, collides with every other such body, and scores `freq ≥ 2` — functions sharing no content counted as repeated structure. | The guard test. | §7.3 |
| R-MEAS-5 | The frozen vacuous-clause list **MAY** be added to and an entry **MUST NEVER** be removed to make the number fall. (Note `Object.freeze` on a `Set` does not prevent `.add()`.) | `engine/clause-quality.js VACUOUS`. | §7.0 |
| R-MEAS-6 | **AMENDED 2026-09-01 — the compression half carried a retired framing.** English-% **MUST** be reported and **MUST NOT** be optimised: a rise achieved by paraphrasing unique code is a regression in disguise. Byte size **MUST** be reported **beside** review surface and **MUST NOT** be called a goal — *it used to read "real lossless compression through recursive word reuse is a goal"*, which §4 retired: **"REVIEW-SURFACE REDUCTION is the goal — not byte compression"** (Amir, 2026-08-31: *"its not about compression"*). Measured, the two move in opposite directions and the retired framing would read the result as a failure: the corpus is **+19% bytes** (.en 4,830,829 B vs .ts 4,058,328 B) and **1,610 top-level / 29,260 whole-tree** review surface against S=33,918. More bytes, far less to read. | §7.1, §7.3; the non-goal is §4. no mechanized row of its own; the nearest enforced neighbour is `verify-register.js` R-ARCH-16, which requires review surface beside byte-identity and cites this row for why a size number reported alone is forbidden. | §3, §4, §7.1 |
| R-MEAS-7 | Residue **MUST** be classified, never papered over: non-recurring shape · free-text slot · comment/trivia · formatting variance. | Layer A reporting. | §4 |
| R-MEAS-8 | A candidate count **MUST NOT** be reported as a collapse count. WIDE-axis tools report *cluster candidates*; only sites passing the byte-exact gate become spans, and the two are not comparable. | Any "how much is left" claim comes from R-MEAS-3's classifier. | §9.1 |
| R-MEAS-9 | The per-file **one-word rate** **MUST** be published by the render producer, beside review surface. | **HOLDS, 2026-09-01** — `en-index.json` now carries `reviewSurface.oneWordFiles` / `oneWordPct` / `filesNotCollapsed` / `worstBySpans`, and `perFile[].topSpans` / `.oneWord`; `write-en-files.js` prints the rate on every run. *Previously:* the target of R-ARCH-15 had **no producer**: `en-index.json` has no `perFile` and no `oneWordFiles`, so the rate had to be measured by an out-of-band script — the R-MECH-8 shape. Check: both keys present; a run that cannot state the rate is not a passing run. | §5D.4A |

## R-TEST — test integrity

| ID | Requirement | Check | Why (§) |
|---|---|---|---|
| R-TEST-1 | Correctness **MUST** assert against real source through a round-trip: the oracle is the corpus itself, `compileFileEn(renderFileEn(src)) === src` over actual files on disk. | §10.1. | §10 |
| R-TEST-2 | A mined artifact **MAY** be an **input**, and **MUST NEVER** be the **oracle**. Grading the engine against a catalog the engine wrote proves only that the engine still agrees with itself. | §10.2. | §10 |
| R-TEST-3 | Every guard **MUST** be mutation-checked at authoring time — disable the assertion, confirm it goes red *with the message it promises*, restore, confirm green — and that **MUST** be stated in the merge request. | §10.3. | §10 |
| R-TEST-4 | Pinning an **inventory** is legitimate; pinning an **answer** is not. A drift guard pins the current inventory so each addition becomes a decision someone makes, and is updated in the same commit with a stated reason. | §10.4. | §10 |
| R-TEST-5 | Where a full-corpus assertion is too slow, a test **MUST** sample deterministically (a fixed, evenly-spread sample) rather than narrowing its oracle. A test that skips is honest; a test that narrows its oracle to pass is not. | §10.5. | §10 |


The register in Part III is the *what*. This part is the *where and how much*: the constants with
their sources of truth, the two protection levels an artifact can have, the executable artifact
contract, the provenance rules, and the two-root configuration model.
